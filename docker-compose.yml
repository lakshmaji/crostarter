version: '3.8'
services:
  cache:
    image: redis:6.2-alpine
    restart: always
    ports:
      - '6379:6379'
    command: redis-server --save 20 1 --loglevel warning --requirepass eYVX7EwVmmxKPCDmwMtyKVge8oLd2t81
    volumes:
      - cache:/data

  # dev_db:
  #   image: postgres:15.4
  #   container_name: cro
  #   environment:
  #     POSTGRES_USER: testuser
  #     POSTGRES_PASSWORD: testpassword
  #     POSTGRES_DB: crodb
  #   ports:
  #     - '5432:5432'
  #   volumes:
  #     - pg-cache:/var/lib/postgresql/data
volumes:
  cache:
    driver: local
  # pg-cache:
  #   driver: local
